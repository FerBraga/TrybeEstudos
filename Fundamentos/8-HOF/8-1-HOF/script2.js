const sorteio = (num, jogo) => {

    const numero = Math.floor(Math.random() * 6);

    return numero;

}
console.log(sorteio());

const jogo = (n) => {
    const numeroJogado = Math.floor(Math.random() * 6);
}